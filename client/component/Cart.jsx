import React , {useState, useEffect} from 'react';


const Cart = () => {
    // create a totalCost hook
    const [show,setShow] = useState(false);
    const [total, setTotal] = useState(0);
    const [numberofItems, setItemTotal] = useState(0);
    // create a items in cart hook

    // useEffect(()=>{
    //     setShow(props.show)
    // },[show])

    return (
        <div>
            
        </div>
    )
}


export default Cart;